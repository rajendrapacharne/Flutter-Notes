void main() {
  add(10,20);
  manager(5,'pants');
}

add(int a,int b)
{
  int sum=0;
  for (int i = 0; i < 5; i++) {
    sum=sum+i;
    print('hello ${i + 1}');
  }
  print(sum);
  print(a+b);
}

manager(int q,String item)
{
  print('$q of $item are ready');
}